'use client'

// Screen sharing is not available with Agora Voice SDK.
// This component is kept as a stub for future use.

interface ScreenTileProps {
  peer: { name?: string; isLocal?: boolean };
}

export const ScreenTile = ({ peer }: ScreenTileProps) => {
  return null;
};
